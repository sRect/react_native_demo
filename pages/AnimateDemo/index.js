import React, {memo} from 'react';
import {View, Text} from 'react-native';

const AnimateDemo = () => {
  return (
    <View>
      <Text>animate</Text>
    </View>
  );
};

export default memo(AnimateDemo);
